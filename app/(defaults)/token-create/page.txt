import React from 'react';
import { Metadata } from 'next';
import { getServerSession } from 'next-auth/next';
import { redirect } from 'next/navigation';

import { authOptions } from '@/lib/auth-options';
import TokenCreateSection from '@/components/token/TokenCreateSection';

export const metadata: Metadata = {
    title: 'Create Token',
};

export const viewport = {
    width: 'device-width',
    initialScale: 1,
};

export default async function Page() {
    const session = await getServerSession(authOptions);

    // If no session, redirect to home page
    if (!session) {
        redirect('/');
    }

    return (
        <div className="px-4 mt-16 mb-32">
            <div className="container mx-auto !max-w-[900px]">
                <div className="text-center base-white text-3xl font-semibold py-4">Create Token</div>
                <TokenCreateSection />
            </div>
        </div>

        // <div className="my-[120px]">
        //     <main className="w-full p-4">
        //         <div className="text-center base-white text-3xl font-semibold">Start A New Token</div>

        //         <div className="text-center mt-6 mb-16">
        //             <ul className="list-none font-semibold space-y-1 text-white">
        //                 <li>
        //                     <p>
        //                         1. <span className="underline">Verify</span> your personal Twitter account
        //                     </p>
        //                 </li>
        //                 <li>
        //                     <p>2. Add at least one social link</p>
        //                 </li>

        //             </ul>
        //         </div>
        //         {process.env.NEXT_PUBLIC_ENV === 'prod' ? <CoinCreateForm /> : <CoinCreateFormWithMilestones />}
        //     </main>
        // </div>
    );
}
